import React from 'react'

export const metadata = {
    title: "Auth page",
    template: "%s | Auth page",
    description: "This is the auth page"
}

function Auth() {
  return (
    <div>Auth</div>
  )
}

export default Auth